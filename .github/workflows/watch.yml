name: watch

on:
  schedule:
    # 정각/5분 정시는 전 세계 워크플로가 몰려 지연이 심하므로 1분 비껴서 등록
    - cron: '1-59/5 * * * *'
  workflow_dispatch:

concurrency:
  group: watch
  cancel-in-progress: false

permissions:
  contents: write  # 봇 명령으로 바뀐 config.yaml 커밋용

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5

      # 수동 실행 시 러너에서 CGV API가 뚫리는지(Cloudflare) 확인용
      - name: API smoke test
        if: github.event_name == 'workflow_dispatch'
        run: uv run cgv-alarm lookup theater 용산

      # actions/cache는 키가 불변이라, 매 실행 고유 키로 저장하고
      # restore-keys 접두사 매칭으로 가장 최근 state를 복원한다
      - name: Restore state
        uses: actions/cache/restore@v4
        with:
          path: state.json
          key: state-${{ github.run_id }}
          restore-keys: |
            state-

      # cron 최소 간격이 5분이라, 잡 하나가 55초 간격 5회 체크로 ~4.6분을 커버해
      # 체감 감시 간격을 ~1분으로 좁히고 cron 지연 시 공백도 줄인다
      - name: Check
        run: uv run cgv-alarm loop --interval 55 --rounds 5
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}

      - name: Save state
        if: always()
        uses: actions/cache/save@v4
        with:
          path: state.json
          key: state-${{ github.run_id }}

      # 텔레그램 /watch, /remove 명령이 config.yaml을 바꿨으면 커밋
      - name: Commit watch changes
        if: always()
        run: |
          if ! git diff --quiet config.yaml; then
            git config user.name "cgv-alarm-bot"
            git config user.email "noreply@github.com"
            git add config.yaml
            git commit -m "bot: 감시 대상 변경"
            git pull --rebase origin main
            git push
          fi
